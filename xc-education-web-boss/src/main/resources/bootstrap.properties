# info
info.eureka.host=localhost
info.eureka.port=5761

# profile
spring.profiles.active=dev

# eureka
eureka.client.serviceUrl.defaultZone=http://${info.eureka.host}:${info.eureka.port}/eureka/

# server
server.port=5880

# application
spring.application.name=xc-education-web-boss

# spring cloud config client
spring.cloud.config.profile=${spring.profiles.active}

spring.cloud.config.label=master
# 是否开启通过service-id来找到config server
spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.service-id=xc-education-server-config